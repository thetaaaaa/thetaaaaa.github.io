# 站点个性化配置

## 修改 `_config.yml`

只需修改顶部关键字段：

| 字段 | 改成 |
|---|---|
| `title` / `title_en` | 你的名字（相同时英文行自动隐藏） |
| `tagline` / `tagline_en` | 首页定位语，中英各一行 |
| `email` | 页脚联系邮箱 |
| `url` | `https://你的用户名.github.io` |

## 修改 `about.md`

将 `about.md` 中的占位内容替换成你的个人介绍。

> 修改 `_config.yml` 后需重新推送才会生效。
